package base;

public class Settings {

    // Maximum threads for a full network scan (CDIR /24)
    public static int maxNetThreads = 256;

    // Maximum wait time for an answer from an ICMP ping
    public static int maxICMPWait = 10000;
}
